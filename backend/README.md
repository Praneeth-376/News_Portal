# Personalized News Portal - Backend

## Quick start
1. Copy `.env` (see .env.example) and set values.
2. Install deps: `npm install`
3. Start dev server: `npm run dev`
4. Start prod: `npm start`

Ports: default `5000`. MongoDB must be running at `MONGO_URI`.

## Tests
`npm test`
